import React from 'react';

const EpisodeStartView = ({ room, onNextClick }) => {
	return (
		<div>
			<h1>Episode Start</h1>

			<button type="button" className="button button-primary" onClick={this.onNextClick}>
				Start Game
			</button>
		</div>
	);
};

export default EpisodeStartView;
